#include <iostream>

using std::cerr;
using std::cin;
using std::cout;
using std::endl;

int main() {
  int a, b; // a / b
  cout << "Input two integers: ";
  cin >> a >> b;
  cout << a / b << endl;
  return 0;
}
