# Thie file is the template for all Makefiles
# DO NOT DELETE THIS FILE

CXX = g++
CXXFLAGS = -pipe -std=c++11 -pedantic-errors -Wall -Wextra -ggdb -lm -I..

all: $(TARGETS)

clean:
	rm -f *\~ .*.un\~
	rm -f *.o

clobber: clean
	rm -f $(TARGETS)

.PHONY: all
